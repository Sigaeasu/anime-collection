import { css } from '@emotion/css'

const linkStyle = css`
    background-color: transparent;
    color: black;
    width: 25%;
    padding: 2px;
    &:hover {
        color: black
    }
`

export default {
    linkStyle
}